apiVersion: apps/v1
kind: Deployment
metadata:
  name: trip-service
  namespace: trip-platform
  labels:
    app: trip-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trip-service
  template:
    metadata:
      labels:
        app: trip-service
    spec:
      containers:
      - name: trip-service
        image: trip-platform/trip-service
        ports:
        - containerPort: 8081
        envFrom:
        - configMapRef:
            name: app-config
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: trip-service
  namespace: trip-platform
spec:
  selector:
    app: trip-service
  ports:
  - port: 8081
    targetPort: 8081
  type: ClusterIP
